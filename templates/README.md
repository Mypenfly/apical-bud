<!-- 模板：讨论根入口。复制为 design/<slug>/README.md，每轮同步"当前状态"。      账本类文件：任何时候都可写（它记录已经发生的事），但不要用它代替对话。
     -->
---
kind: index
stage: S0
updated: YYYY-MM-DD
---

# <主题> · 需求推演

**当前阶段**：S0 种子 · 需求对齐
**树**：层 0（已确认 0） · 推演节点 0（候选 0 / 定论 0 / 淘汰 0） · 未决问题 0
**结论**：推演进行中（尚无需求定稿，尚无顶芽）

## 一句话现状

<三行以内：种子是什么、现在推到哪一跳、下一步问什么。>

## 这棵树

| 部位 | 文件 | 状态 |
|---|---|---|
| 种子 | `seed/real-need.md` | 未确认 |
| 根系（拆词与认领） | `seed/real-need.md` 的「拆词与认领」+ `layers/` | 未开始 |
| 主干（推演：候选 ⇄ 定论） | `derivation/` | 未开始 |
| **需求定稿**（第一跳的落点） | `concept/need.md` | 未开始 |
| 顶芽（唯一的理念概念，第二跳的落点） | `concept/concept.md` | 未开始 |
| 侧枝（机制命题 + 概念延伸） | `concept/extensions.md` | 未开始 |
| 果实（技术方案） | `tech/selection.md` | 未开始（S5 之后） |
| 肥料（调研） | `derivation/` 各节点的“调研佐证” | —— |
| 生命力（问答） | `questions/`、`rounds/` | —— |

## 怎么读这棵树

- **推演分两跳，每跳先摊候选再挑一个**：第一跳把需求说定（`concept/need.md`），第二跳才定理念（`concept/concept.md`）。跳过第一跳直接谈理念，谈的就是现成方案，不是你的需求。
- **层从你的话里来**：每个层都对应种子某个词的一项，由你认领为"核心"。我们自己切的角度写在层文件的「其他解读（被否）」里，不冒充你的需求。
- **推演，不是灵感**：`kind: 定论` 的节点都写清它从哪个父节点推出来；祖先链断掉的定论不是推演，是插进来的想法。`kind: 候选` 不查这条——摊候选时本来就还没有论证。
- **气质在顶芽，保证在侧枝**：`concept.md` 是这一屏的意向（可解释、能生成）；`M-` 机制命题是"靠什么保证它"，技术选型照着 `M-` 走。
- 被否的枝不删：淘汰的候选保留在 `derivation/`（带淘汰理由与**复活条件**），层文件里被否的解读也保留。
- **你可以反悔**：任何已定的东西都能撤回，不需要理由。旧内容不删，标 `superseded-by:`；受影响的判据标 `stale: true`，由 `audit/recheck-*.md` 结算。
- 决策在 `decisions/`，异议在 `audit/challenges.md`（含被驳回的），裁决在 `audit/verdict.md`。
- 术语表 `glossary.md` 是**你和我的共同语言**：你可以改写任何术语，我负责全局替换并记下曾用名。
- 机器可读状态在 `state.json`；门禁判定以文件和它为准，不以对话记忆为准。
