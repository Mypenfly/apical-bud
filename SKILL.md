---
name: apical-bud
description: "文件驱动的理念推演方法论：从一句可能模糊的需求（种子）出发，分层（根系）、逐层推演（主干），收敛到唯一一个核心理念（顶芽）及其延伸（侧枝），定稿后再据需求/扩展性/应用场景推出技术方案（果实）；调研是肥料，问答是生命力。主持人会质疑你的提议，任何阶段都有权否决。Concept-deduction methodology for pre-development discussion — seed, layers, derivation, one apical concept, extensions, then technology selection."
whenToUse: "用户想在想清楚做什么之前先讨论（我们聊聊这个想法 / 帮我理清需求 / 这个概念该怎么设计 / 这个方向值得做吗），或要评审一个尚未成形的产品、系统构想时。"
---

# apical-bud（顶芽）

在写第一行代码之前，把"要做什么"推演清楚。产物是一棵**理念推演树**：

```
种子 seed/real-need.md ──► 根系 layers/ ──► 主干 derivation/ ──► 顶芽 concept/concept.md
  一句话真实需求            需求分层          理念推演节点          唯一的核心理念
                                                                      ├─ 侧枝 concept/extensions.md
                                                                      └─ 果实 tech/selection.md

调研 = 肥料（供给前提与佐证，不代替生长）      问答 = 生命力（树每长一节，都经过一轮问答）
```

## 三条承重规则（门禁会查，不是修辞）

1. **没有父节点的不算概念。** 每个 `N-` 节点的 `from:` 必须指向存在的层或节点，且祖先链必须能走到种子。灵感可以被讨论，但要先变成某一层的推演才能进主干。
2. **顶芽只有一个。** 收敛 = 选出一个核心理念，并把竞争节点写进「淘汰的竞争节点」；淘汰的枝留在树上（`status: dropped` + 理由），不删。
3. **不确认不长。** 层没有用户确认就不许挂推演节点；术语没确认不许当既定概念用。

## 阶段机

| 阶段 | 名称 | 目标 | 细则 |
|---|---|---|---|
| S0 | 种子 | 一句话真实需求（≤80 字，用户确认）+ 被否的表述 + 非目标 | `references/seed-and-layering.md` |
| S1 | 根系 | 需求分层：每层有 parent、分解理由、其他解读（被否）、判据、用户确认 | `references/seed-and-layering.md` |
| S2 | 推演 | 逐层生长：每个层至少一个推演节点，保留节点带反例、淘汰节点带理由 | `references/derivation.md` |
| S3 | 顶芽 | 收敛为唯一核心理念：一句话 + 关键名词 + 边界 + 非目标 + 反例 + 分层覆盖 | `references/apical.md` |
| S4 | 侧枝 | 延伸三档（必然/需求/猜测） | `references/apical.md` |
| S5 | 定型 | 理念定稿：final + 推演链无断链 + 已知反对与回应 + 红队复核 | `references/apical.md` |
| S6 | 果实 | 选型判据：先锁判据（来源必须是种子/层/节点） | `references/tech-selection.md` |
| S7 | 果实 | 选型定稿：候选带证据等级与推演来源 + 退出成本 + 未验证假设 | `references/tech-selection.md` |

- **不得跳阶**，也**不得提前选型**（S0–S2 期间不讨论语言/框架/库）。
- **回退是正常的**：推不出自洽节点 → 回上一层；分层错了 → 回 S1；种子站不住 → 回 S0。回退要记进轮次纪要。
- 推进阶段必须通过门禁：DSH 里用 `apical_gate` 工具（`status`/`check`/`advance`/`verdict`）；任何宿主里都能用 `scripts/gate.mjs`。**没通过门禁不许说"本阶段完成"。**

## 每轮循环（七步）

```
① 读盘 state.json + README + 相关层/节点/问题
② 声明：当前阶段 · 阻塞本阶段的未决问题 · 本轮关闭哪一个
③ 提问：ask_user_question，一个问题，2–4 个选项带代价
④ 复述：1–2 句确认记下了什么（含 ID）
⑤ 生长：写或改 种子/层/推演节点
⑥ 记账：rounds/round-NNN.md → 术语变动 → state.json（round++）
⑦ 收尾：门禁通过就 check → advance，否则报出下一个问题
```

一轮只关闭一个问题、最多长一节。完整协议、提问纪律与反模式见 `references/protocol.md`。

## 调研的定位（肥料）

调研供给**前提**、检验**假设**、提供**反例**，但不产出结论。禁止"因为某项目这么做，所以我们的理念是 X"——结论必须从层或节点推出来。调研事实与推演冲突时，回到那个节点的父层修正，而不是把调研结论硬塞进顶芽。

## 术语与措辞（防"看不懂的墙"）

- 一轮最多引入 **1 个**新术语；写进 `glossary.md` 标 `[提案]`，用户确认后改 `[确认]`；`[提案]` 同时最多 1 个（门禁阻塞项）。
- 定义必须是**人话**，且不能用另一个尚未定义的术语解释自己（门禁检查）。
- 顶芽一句话里的自造词（关键名词）最多 5 个，且必须都是已确认术语。
- **用户可以改写任何东西**：问题、术语、措辞。改写后以用户的措辞为准，全局替换，并记下曾用名/曾用措辞。用户说"这堆词我看不懂"时，停下推演先改词。

## 质疑与否决

出现下列情形必须当轮提出反对，并写进 `audit/challenges.md` 的 `## X-00N`（格式：主张 → 证据 → 代价 → 替代方案 → 可证伪判据）：

- 种子自相矛盾（"全自动又每一步可控"）；
- 某一层推不出任何自洽节点（可能分层错了）；
- 推演需要引入第二套机制才能覆盖所有层（可能有两个顶芽）；
- 收益明显小于成本，或已有更简单方案；
- 合规 / 安全 / 维护性硬伤；
- 同一问题 ≥3 轮无新信息（原地打转）。

**建议终止**是正当产出：写 `audit/verdict.md`（`## V-00N`）并用 `apical_gate action=verdict status=stop` 记录，然后明确告诉用户。**"推演失败"也是正当产出**：推不动就说推不动，不要硬造概念把树撑起来。

## 资源

| 路径 | 用途 |
|---|---|
| `references/protocol.md` | 每轮循环、提问纪律、用户改写流程、反模式 |
| `references/seed-and-layering.md` | S0–S1 种子对齐与需求分层 |
| `references/derivation.md` | S2 推演规则、调研的正确用法、回退时机 |
| `references/apical.md` | S3–S5 收敛、侧枝、定稿与红队复核 |
| `references/tech-selection.md` | S6–S7 判据、证据分级、退出成本 |
| `references/doc-conventions.md` | 目录 / ID / frontmatter / 标题 / 术语表 / state.json（门禁契约） |
| `templates/` | 17 份可直接复制的文档模板 |
| `scripts/gate.mjs` | 零依赖门禁校验器（模块 + CLI） |
