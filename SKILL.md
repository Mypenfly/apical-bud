---
name: apical-bud
description: "文件驱动的需求推演方法论：从一句可能模糊的需求（种子）出发，先把它拆开、由用户逐项认领（根系），再推演两跳——第一跳把需求说定（需求定稿），第二跳定出唯一一个核心理念（顶芽），然后写延伸（侧枝），定稿后再据判据推出技术方案（果实）；每跳都是先摊开候选、再挑一个。调研是肥料，问答是生命力。主持人会质疑你的提议，任何阶段都有权否决。Concept-deduction methodology for pre-development discussion — seed, word-by-word claiming, two rounds of candidate-then-conclusion, one apical concept, extensions, then technology selection."
whenToUse: "用户想在想清楚做什么之前先讨论（我们聊聊这个想法 / 帮我理清需求 / 这个概念该怎么设计 / 这个方向值得做吗），或要评审一个尚未成形的产品、系统构想时。"
---

# apical-bud（顶芽）

在写第一行代码之前，把"要做什么"推演清楚。产物是一棵**需求推演树**：

```
种子 seed/real-need.md ─► 根系 layers/ ─► 主干 derivation/ ─► 需求定稿 concept/need.md
  一句话真实需求           拆词与认领      候选 ⇄ 定论，两跳    一句话，用户指着说"就是这个"
                                                                    │
                                                        顶芽 concept/concept.md
                                                          唯一的理念概念（意向）
                                                                    ├─ 侧枝 concept/extensions.md
                                                                    │    ├─ M- 机制命题（可证伪，落为选型限制）
                                                                    │    └─ E- 概念延伸（档位）
                                                                    └─ 果实 tech/selection.md

推演分两跳，两跳各做一次「摊开候选 → 挑一个」：
  第一跳（S2）这项需求到底指什么 → 需求定稿      第二跳（S3）这一屏该是什么气质 → 顶芽

调研 = 肥料（供给前提与佐证，不代替生长）      问答 = 生命力（树每长一节，都经过一轮问答）
```

**一句话分工**：需求定稿回答"你要的到底是什么"（用户能指着说"对，就是这个"）；顶芽回答"这一屏是什么气质"（可解释、能生成，不必可证伪）；机制命题回答"靠什么保证"（可证伪、带反例）；果实回答"用什么实现"（判据先锁）。

## 对话是介质，文件是留痕（最高优先）

- **对话承载内容**：提案、选项、推演链、理由、代价、反例，都要说在对话里——用户不看任何文件就能判断和回答。
- **文件只做留痕**：只记录已经发生的事（用户说过的话、已确认的层、已淘汰的节点）。**没有答复的提案不落盘。**
- **自检**：如果用户必须打开文件才能回答，就是我违规了——回到对话里讲清楚再问。提问里出现 ID 时必须附上它的人话内容。
- **写文件的三种合法时机**：① 拿到用户答复后留痕；② 用户明确要求出文档；③ 补机械记录（轮次纪要、state.json、用户原话）。
- DSH preset 的写守卫会拦下「本轮答复之前新建讨论文件」的写入；被拦是提醒，不是障碍。

## 三条承重规则（门禁会查，不是修辞）

1. **没有出处的不算推演。** 每个 `N-` 节点的 `from:` 必须指向存在的层、节点或 `need`；`kind: 定论` 还必须能走到种子。**`kind: 候选` 只要求 `from` 存在**——摊候选时还没有论证，这正是候选与定论的区别。凭空冒出来的东西，两条都不算。
2. **顶芽只有一个，机制可以有好几个。** 理念概念唯一；机制命题写进侧枝，允许互相竞争，也允许被复活。淘汰的枝留在树上（`status: dropped` + 理由 + 复活条件），不删。
3. **不确认不长。** 层没有用户确认就不许挂推演节点；词的构成项没有用户认领就不算层；术语没确认不许当既定概念用；反悔只认用户说过的话。

## 阶段机

| 阶段 | 名称 | 目标 | 细则 |
|---|---|---|---|
| S0 | 种子 | 一句话真实需求（≤80 字，用户确认）+ 被否的表述 + 非目标 + **判定对齐的信号** | `references/seed-and-layering.md` |
| S1 | 根系 | **拆词与认领**：种子里说不清就没法动手的词逐个拆成项，用户逐项标 `核心/次要/不算`；认下来的写成层 | `references/seed-and-layering.md` |
| S2 | 推演 | **第一跳**：摊开候选（`kind: 候选`，带 `## 对立面`、`## 放弃了什么`）→ 挑定论 → **需求定稿** `concept/need.md` | `references/derivation.md` |
| S3 | 顶芽 | **第二跳**：从需求定稿再摊一次候选 → 收敛为唯一的**理念概念**：名字 + 一句话 + 它生成的主张 | `references/derivation.md` + `references/apical.md` |
| S4 | 侧枝 | **机制命题 `M-`**（可证伪、给定边界）+ **概念延伸 `E-`**（必然/需求/猜测） | `references/apical.md` |
| S5 | 定型 | 概念定稿 + 机制成文 + 全量重判 + 红队复核 | `references/apical.md` |
| S6 | 果实 | 选型判据：先锁判据（来源是种子/**需求定稿**/层/节点/**机制命题**） | `references/tech-selection.md` |
| S7 | 果实 | 选型定稿：候选带证据等级与推演来源 + 退出成本 + 未验证假设 | `references/tech-selection.md` |

- **不得跳阶**：**不许跳过第一跳直接想理念**——跳过它，摊开的就还是我知识库里的现成方案，不是你的需求。也**不得提前选型**（S0–S3 期间不讨论语言/框架/库）。
- **一轮只做一个动作**：摊开候选算一轮，挑定论算另一轮。摊开那一轮不落任何定论。
- **全量重判**：进 S3、S5、S6、S7 之前，把 `status: dropped` 的候选与定论、被淘汰的主张、未验证假设逐条重判一遍，结果写 `audit/recheck-<stage>.md`（没变也要写一句为什么）。**没做重判不许 advance**。
- **回退是正常的**：某项摊不出对立的候选 → 回 S1 重新拆词；需求定稿用户不认 → 回 S2 补第一跳；种子站不住 → 回 S0。回退要记进轮次纪要。
- 推进阶段必须通过门禁：DSH 里用 `apical_gate` 工具（`status`/`check`/`advance`/`verdict`，另有 `trees` 列出项目里的全部树、`bind` 把本会话绑定到某一棵）；任何宿主里都能用 `scripts/gate.mjs`。**没通过门禁不许说"本阶段完成"。**
- **一个项目可以有多棵树**（不同需求、不同阶段，树与树可以有关联）：一个会话绑定一棵；一棵树结束后开新树，并用 `state.json` 的 `dependsOn` / `relation` 与种子的「## 与已有树的关系」记录关联；跨树引用写成 `<slug>#L-003`。

## 每轮循环（七步）

```
① 读盘 state.json + README + 相关层/节点/问题
② 对话里用三句话重述：当前阶段 · 本轮要定什么 · 阻塞本阶段的未决问题
③ 对话里给出完整提案：候选、推演链、代价、反例（用户不看文件就能判断）
④ 提问：ask_user_question，一个问题，2–4 个选项带代价，**并写明我的倾向与它的代价**
⑤ 拿到答复 → **先判落点**（选项内 / 改写我的选项 / 选项外的新内容 / 只是重申 / 与树上冲突）
   → 再与已确认的层和节点比对 → 复述确认：1–2 句，含 ID
⑥ **这时才落盘**：层/节点/决策 → rounds/round-NNN.md → 术语表 → state.json（round++）
⑦ 收尾：门禁通过就 check → advance，否则报出下一个问题
```

一轮只关闭一个问题，最多长一节。**③ 在 ⑥ 之前**：先把内容说清、拿到答复，才写文件。

**⑤ 是全流程最容易滑坡的一步**：用户没选我给的选项、自己写了一段话时，我不许直接照办——先答一句"我的选项集错在哪"，再跟树上比一遍。完整协议、提问纪律、禁止短语表与反模式见 `references/protocol.md`。

## 调研的定位（肥料）

调研供给**前提**、检验**假设**、提供**反例**，但不产出结论。禁止"因为某项目这么做，所以我们的理念是 X"——结论必须从层或节点推出来。调研事实与推演冲突时，回到那个节点的父层修正，而不是把调研结论硬塞进顶芽。

**一条推演依赖"某个程序或运行时到底怎么表现"时，当轮做三选一**（选哪条写进节点）：

1. **读源码/文档/日志**——写出文件+行号或函数名；写不出就是没读。
2. **承认没读**——明写"我没读 X"，并降级为反例或未验证假设（`evidence: E3`）。**不许再拿它当硬要求去筛候选。**
3. **必须真机实测**——只有前两条穷尽后才走：给用户**交接三件套**（交接文档 + 一段可直接复制的提示词 + 通过/不通过的标准），结果写进 `tech/probes/`。

**禁止把 ① 该做的事推给 ③**：读一次源码就能得到答案的，不许让用户重启机器。

## 术语与措辞（防"看不懂的墙"）

- 一轮最多引入 **1 个**新术语；写进 `glossary.md` 标 `[提案]`，用户确认后改 `[确认]`；`[提案]` 同时最多 1 个（门禁阻塞项）。
- 定义必须是**人话**，且不能用另一个尚未定义的术语解释自己（门禁检查）。
- **字段名与标题也算术语。** 文档里出现的每个小节标题、每个 frontmatter 字段，用户都要能一眼看懂；新增一个之前先问一次"这个说法你会怎么叫"。**看不懂的字段名和看不懂的自造词一样有害。**
- **不用比喻堆概念。** 树的那几个词（种子/根系/主干/顶芽/侧枝/果实）是已经说定的，不要再添新的比喻；新机制一律用平实说法命名。
- **理念概念用日常语言命名**（"纸墨""门厅""留白"），自造词不超过 3 个且必须已确认——名字本身就要能解释自己。
- **用户可以改写任何东西**：问题、术语、措辞、字段名。改写后以用户的措辞为准，全局替换，并记下曾用名/曾用措辞。用户说"这堆词我看不懂"时，停下推演先改词。
- **用户可以反悔**：任何已定的东西（层、节点、机制、判据、选型）都能撤回，不需要理由。旧文档不删、标 `superseded-by:`；新条目标 `supersedes:`；受影响的判据点标 `stale: true`，由重判结算。但**要当场说清哪个节点会因此倒掉**——这不是阻挠，是尽职。

## 质疑与否决

出现下列情形必须当轮提出反对，并写进 `audit/challenges.md` 的 `## X-00N`（格式：主张 → 证据 → 代价 → 替代方案 → 可证伪判据）：

- 种子自相矛盾（"全自动又每一步可控"）；
- 某一项摊不出任何互相对立的候选（可能拆词拆错了）；
- **用户给的答复与已确认的层/节点冲突**（这是"用户坚持"唯一能被测到的时刻，不许静默采纳）；
- **用户把一件事降级成"约束"**（"不因它而牺牲就行"）——它可能其实是一个层；
- 需求定稿里有一条成分没被任何候选接住，或顶芽接不住需求定稿；
- 推演需要引入第二套机制才能覆盖全部（可能有两个顶芽）；
- 收益明显小于成本，或已有更简单方案；
- 合规 / 安全 / 维护性硬伤；
- 同一问题 ≥3 轮无新信息（原地打转）。

**建议终止**是正当产出：写 `audit/verdict.md`（`## V-00N`）并用 `apical_gate action=verdict status=stop` 记录，然后明确告诉用户。**"推演失败"也是正当产出**：推不动就说推不动，不要硬造概念把树撑起来。

## 资源

| 路径 | 用途 |
|---|---|
| `references/protocol.md` | 每轮循环、提问与选项纪律、**答复落点判法**、禁止短语表、反悔流程、全量重判、反模式 |
| `references/seed-and-layering.md` | S0–S1 种子对齐、**拆词与逐项认领**、信号追踪 |
| `references/derivation.md` | S2–S3 候选/定论两档、**两跳推演**、需求定稿、三选一裁决、清单来源、回退时机 |
| `references/apical.md` | S3–S5 理念概念、机制命题与延伸、定稿与红队复核 |
| `references/tech-selection.md` | S6–S7 判据、候选、证据分级、退出成本 |
| `references/doc-conventions.md` | 目录 / ID / frontmatter / 标题 / 术语表 / state.json（门禁契约） |
| `templates/` | 20 份可直接复制的文档模板（`need.md` 是需求定稿，`extensions.md` 里含 `M-` 机制命题写法；另有 `recheck` / `handoff`） |
| `scripts/gate.mjs` | 零依赖门禁校验器（模块 + CLI） |
